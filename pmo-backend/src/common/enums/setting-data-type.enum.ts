/**
 * Setting Data Type Enum
 * Maps to PostgreSQL: setting_data_type_enum
 */
export enum SettingDataType {
  STRING = 'STRING',
  NUMBER = 'NUMBER',
  BOOLEAN = 'BOOLEAN',
  JSON = 'JSON',
  DATE = 'DATE',
  DATETIME = 'DATETIME',
}
