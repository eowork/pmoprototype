/**
 * Document type enum - used for categorizing documents
 * Used by: documents table
 */
export enum DocumentType {
  CONTRACT = 'CONTRACT',
  REPORT = 'REPORT',
  POLICY = 'POLICY',
  SPECIFICATION = 'SPECIFICATION',
  PROPOSAL = 'PROPOSAL',
  MINUTES = 'MINUTES',
  MEMO = 'MEMO',
  OTHER = 'OTHER',
}
