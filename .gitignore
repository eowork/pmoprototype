node_modules/
.env
.DS_Store
dist/
build/
nul

# Temporary Claude/AI session files
**/tmpclaude-*-cwd
.claude/
.trae/

# MCP configuration files (may contain API keys)
.vscode/mcp.json
.mcp.json

# Runtime / operational artifacts (not source code)
database/backups/
database/staging/
pmo-backend/uploads/
prototype/

# Local-only documentation (research, planning, internal notes — not shared via Git)
docs/
# Real-data migration dumps (PII/bcrypt) — transferred out-of-band, never committed
database/migration-data/
